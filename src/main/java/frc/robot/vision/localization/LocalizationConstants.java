package frc.robot.vision.localization;

import edu.wpi.first.math.geometry.Transform3d;

/**
 * Camera-to-mechanism offsets for the vision localization pipeline.
 * Placeholder values — measure from CAD in Stage 8-2.
 */
public class LocalizationConstants {
  /** Offset from turret axis-of-rotation to the turret camera. PLACEHOLDER — Stage 8-2. */
  public static final Transform3d kTurretAoRToTurretCameraOffset = new Transform3d();
}
